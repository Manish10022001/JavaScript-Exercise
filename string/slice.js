//slice : reads and returns the chars between the specified index
let str="Welcome";
console.log(str.slice(2,4)); //start_index = 2, end_index=4 
console.log(str.slice(4)); //start_index=4, till last character